# ════════════════════════════════════════════════════════════
# AI-Empowered Fintech Fraud Detection System
# Python Dependencies — Production & Local Ready (Updated)
# ════════════════════════════════════════════════════════════

# ─── ML & Data Science Stack (Phase 1) ────────────────────
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0
joblib>=1.3.0
shap>=0.43.0

# ─── MLOps & Caching (Phase 4) ────────────────────────────
mlflow>=2.12.0
dvc>=3.0.0
redis[hiredis]>=5.0.0        # hiredis adds high-performance parser for Redis

# ─── Dashboard (Phase 1) ───────────────────────────────────
streamlit>=1.28.0
plotly>=5.18.0

# ─── Observability (Phase 6) ───────────────────────────────
prometheus-fastapi-instrumentator>=6.1.0
structlog>=24.1.0

# ─── Web Framework (Phase 4) ───────────────────────────────
fastapi>=0.104.0
uvicorn[standard]>=0.24.0    # Server runner of FastAPI 

# ─── Database (Phase 6) ────────────────────────────────────
sqlalchemy>=2.0.0            # ORM — Python classes → SQL tables
alembic>=1.13.0              # Database migrations (schema changes track)
psycopg2-binary>=2.9.9       # PostgreSQL database adapter (Required for MLflow DB tracking)
asyncpg>=0.29.0              # Async PostgreSQL driver (Required for FastAPI high-throughput)
greenlet>=3.0.0              # Required for async SQLAlchemy orchestration

# ─── API Validation (Phase 4) ─────────────────────────────
pydantic[email]>=2.4.0       # Data validation + email validation
pydantic-settings>=2.0.0     # BaseSettings for config from .env (Pydantic v2 split)
python-multipart>=0.0.6      # Form data support (for Auth/Login tokens)

# ─── Security & Hashing (Phase 4) ──────────────────────────
python-jose[cryptography]>=3.3.0   # JWT tokens encryption
passlib[bcrypt]>=1.7.4             # Password hashing framework
bcrypt>=4.0.1                      # Explicit bcrypt engine (Fixes Windows/Python 3.11+ passlib bugs)
slowapi>=0.1.9                     # Rate limiting (prevents brute-force attacks)

# ─── HTTP Client (n8n webhook) ──────────────────────────────
httpx>=0.25.0                # Async HTTP requests (for calling n8n triggers)

# ─── Utilities ─────────────────────────────────────────────
python-dotenv>=1.0.0         # for loading the environment variables from .env file

# ─── QA & Testing ──────────────────────────────────────────
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
testcontainers[postgres]>=3.7.1
locust>=2.15.0
faker>=20.0.0
factory_boy>=3.3.0
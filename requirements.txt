# ════════════════════════════════════════════════════════════
# AI-Empowered Fintech Fraud Detection System
# Python Dependencies — Updated for Phase 6 (Database)
# ════════════════════════════════════════════════════════════

# ─── ML & Data Science Stack (Phase 1) ────────────────────
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0
joblib>=1.3.0
shap>=0.43.0

# ─── Dashboard (Phase 1) ───────────────────────────────────
streamlit>=1.28.0

# ─── Web Framework (Phase 4) ─────────────────────────
fastapi>=0.104.0
uvicorn[standard]>=0.24.0    # Server runner of FastAPI 

# ─── Database (Phase 6) ──────────────────────
sqlalchemy>=2.0.0            # ORM — Python classes → SQL tables
alembic>=1.12.0              # Database migrations (schema changes track)

# ─── API Validation (Phase) ─────────────────────────────
pydantic[email]>=2.4.0       # Data validation + email validation
python-multipart>=0.0.6      # Form data support

# ─── Security (Phase 4) ──────────────────────────────
python-jose[cryptography]>=3.3.0   # JWT tokens
passlib[bcrypt]>=1.7.4             # Password hashing

# ─── HTTP Client (n8n webhook) ───────────────────────
httpx>=0.25.0                # Async HTTP requests (for calling n8n)

# ─── Utilities ────────────────────────────────────────────────
python-dotenv>=1.0.0         # for loading  the environment variables from .env file
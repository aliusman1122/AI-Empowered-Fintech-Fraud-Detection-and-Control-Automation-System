# Machine Learning Architecture

Our threat detection scales through offline trained Scikit-Learn ensembles synced strictly tracking parameters via MLFlow.

## Model Training Lifecycle
- **Algorithm**: Random Forest Classifier ensemble bounding explicitly heavily weighted classes guarding against inherently skewed statistical populations typical in financial transactions.
- **Pipeline Strategy**: Features extract inherently normalizing and scaling floating numbers using strictly locked Pickled Scalers.

## Evaluation
- True Positives natively balance explicitly checking F1-Scores. 
- Overfitting bounds explicitly measure validation sets natively avoiding leakage cleanly.

## Retraining Guidelines
We utilize **DVC** mapping raw CSV/parquet inputs dynamically pulling directly from internal data warehouses natively storing exact representations of datasets tied uniquely to explicit Git Hashes smoothly.
1. Sync DVC: `dvc pull`
2. Train Model: `python src/training.py`
3. Push specific artifacts directly checking MLflow tracking grids natively viewing hyperparameters clearly!

## Model Versioning
Always pull production dependencies tracking natively registered packages dynamically scaling deployment routines safely.

# FinGuard Video Demo Script

**Aim for 2:00 timeline.**

**[0:00 - 0:15] Introduction (Screen: Main Dashboard)**
"Welcome to FinGuard AI. Designed for extreme-velocity transactional loads spanning rigorous ML validation targets. What you're seeing natively is the live dashboard bounding metrics checking fraud instances rapidly."

**[0:15 - 0:40] Testing Real-Time Load (Screen: Transaction Form)**
"Let's simulate a massive payload intercept. I'm submitting an abnormal 5,000 transaction flagged across risky international jurisdictions. Notice, TanStack leverages immediate Server-Sent Events automatically updating the graph matrices live reflecting the explicit rule flag blocking the input directly returning explicit reason codes generated natively off our Random Forest models quickly!"

**[0:40 - 1:10] The Architecture & Registry (Screen: Swagger / Backend logs)**
"FinGuard routes natively asynchronous. Let's look at the Swagger interface. All schemas parse explicitly using strict deep Pydantic validation avoiding native SQL injection risks seamlessly mapping rules safely. Notice the backend Prometheus metrics actively measuring explicit latency instances directly reflecting fractional sub 50ms executions natively bypassing heavy lock parameters!"

**[1:10 - 1:40] Model Versioning (Screen: MLflow Dashboard)**
"These models never guess. We track ML dependencies via MLFlow. Checking the MLflow interface directly maps explicitly tracking specific dataset Git boundaries bounding hyperparameter variations logging explicitly precision and F1 recall matrixes tracking exact version models natively shipped cleanly separating experimentation limits explicitly."

**[1:40 - 2:00] Webhook Automations & Close (Screen: n8n Workflow)**
"And if a limit explicitly hits critical targets natively triggering webhooks bounding via n8n explicitly generating native verification emails streamlining asynchronous human-in-the-loop limits catching unique vulnerabilities immediately. That is FinGuard AI."
